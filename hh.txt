/* Responsive untuk layar kecil */
@media (max-width: 600px) {
    :root {
        --card-size: 50px; /* Gambar lebih kecil di layar kecil */
    }

    /* Tier tetap terlihat rapi */
    .tier {
        flex-direction: column;
        align-items: center;
        padding: 8px;
    }

    .label {
        width: 80px;
        font-size: 0.75rem;
        padding: 5px;
    }

    /* Cards menjadi grid agar lebih rapi */
    .cards {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(50px, 1fr));
        gap: 8px;
        justify-items: center;
        padding: 1rem;
    }

    img{
        width: var(--card-size);
    }
}

/* Responsif untuk Tablet */



@media (max-width: 500px){
    :root {
        --card-size: 50px; /* Reduce card size on small screens */
    }

    /* FIX: Ensure tiers stay in a row layout */
    .tier {
        display: flex;
        flex-direction: row; /* Force horizontal layout */
        align-items: center;
        justify-content: space-between;
        gap: 5px;
        padding: 5px;
    }

    /* FIX: Ensure label and controls stay side by side */
    .label {
        width: 60px; /* Reduce label width */
        font-size: 0.75rem;
        text-align: center;
        padding: 5px;
        flex-shrink: 0; /* Prevent shrinking */
    }

    /* FIX: Controls should be smaller and align horizontally */
    .controls {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 40px; /* Reduce button size */
    }

    .controls button {
        font-size: 0.85rem;
        padding: 3px;
    }

    /* Items should wrap nicely and not stretch */
    .items {
        flex: 1; /* Allow items to take remaining space */
        display: flex;
        flex-wrap: wrap;
        gap: 3px;
    }

    /* FIX: Cards container should have a flexible layout */
    .cards {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(50px, 1fr));
        gap: 8px;
        justify-items: center;
        padding: 1rem;
    }
}

@media (min-width: 501px) and (max-width: 600px) {
    :root {
        --card-size: 60px; /* Slightly larger cards for mid-sized screens */
    }

    /* Keep tiers in a row layout */
    .tier {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        gap: 6px;
        padding: 6px;
    }

    /* Label should stay compact */
    .label {
        width: 80px;
        font-size: 0.85rem;
        text-align: center;
        padding: 6px;
        flex-shrink: 0;
    }

    /* Controls should be in a row */
    .controls {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 50px;
    }

    .controls button {
        font-size: 1rem;
        padding: 4px;
    }

    /* Ensure Items and Cards use flex (fixes drag & drop issue) */
    .items, .cards {
        display: flex !important;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: center;
        align-items: center;
    }

    .cards {
        padding: 1.2rem;
    }

    img {
        width: var(--card-size);
    }
}